version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
    cache:
      paths: []
